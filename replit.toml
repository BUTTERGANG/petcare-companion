run = "uvicorn backend.main:app --host 0.0.0.0 --port 8000"

[nix]
channel = "stable-24_05"

[deployment]
run = ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000"]

[deployment.env]
PYTHONPATH = "."